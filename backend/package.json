{
  "name": "@pentest-ide/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud backend for the Pentest IDE: sessions, tools registry, and streamed runs.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/sensible": "^5.6.0",
    "@types/dockerode": "^4.0.1",
    "@types/pg": "^8.20.0",
    "dockerode": "^5.0.0",
    "fastify": "^4.28.1",
    "pg": "^8.20.0",
    "pino": "^9.4.0",
    "pino-pretty": "^13.1.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
